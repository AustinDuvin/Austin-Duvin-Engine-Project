#pragma once
#include <GL\glew.h>
#include <GLFW\glfw3.h>
#include <FreeImage.h>
#include <glm\glm.hpp>
#include <glm\gtx\transform.hpp>
#include <glm\gtx\euler_angles.hpp>
#include <string>
#include <fstream>
#include <iostream>
#include <vector>
#include <sstream>

#include "Timer.h"
class Rigidbody
{
/*private:
	GLFWwindow *window;
	int controlLeft;
	int controlRight;
	int controlUp;
	int controlDown;
	float mass;
	glm::vec3 velocity;
	glm::vec3 acceleration;
	glm::vec3 force;
	bool controllable;
public:
	Rigidbody();
	Rigidbody(GLFWwindow *window, float mass, Transform transform, bool controllable, glm::vec3 force);
	Rigidbody(GLFWwindow *window, int controlLeft, int controlRight, int controlUp, int controlDown, float mass, Transform transform, bool controllable);
	~Rigidbody();

	void update();
	void move();*/
};

